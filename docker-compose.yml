services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - ./api/.env
    environment:
      NOMINATIM_URL: http://nominatim:8080
    restart: unless-stopped
    networks:
      - gat4-net

networks:
  gat4-net:
    name: gat4-net
    external: true
